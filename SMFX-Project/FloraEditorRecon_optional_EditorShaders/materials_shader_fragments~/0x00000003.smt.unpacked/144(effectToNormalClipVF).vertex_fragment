vertexFragment effectToNormalClipVF
	input position

	declareCode
		extern uniform float4x4 modelToClip;
	endCode

	code
		half4 clipPos;
		{
		clipPos = mul( half4( In.position.xyz, 1 ), modelToClip );
		clipPos.xyz /= clipPos.w;
		clipPos.w = 1;
		}
	endCode
end
